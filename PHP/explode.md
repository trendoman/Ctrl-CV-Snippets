# explode

```php
$lines = explode( "\n", $text );
foreach( $lines as $line ){
    //$wrapped_line = '<code>'.trim($line).'</code>';
    //$html .= str_replace( trim($line), $wrapped_line, $line);
}
```
